import { Meta, Story, Canvas, ArgsTable, Source } from '@storybook/addon-docs'
import {
	ArrowDown,
	ArrowLeft,
	ArrowRight,
	ArrowUp,
	BackSpace,
	Close,
	Code,
	Github,
	Heart,
	Like,
	Member,
	Members,
	Sharing,
	XCircle,
  Android,
	Angular,
	Aws,
	C,
	Cpp,
	Cs,
	Css,
	Deno,
	Django,
	Docker,
	Flutter,
	Go,
	Html,
	Java,
	Javascript,
	Kotlin,
	Kubernetes,
	Linux,
	Nodejs,
	Python,
	R,
  React as ReactIcon,
	Ruby,
	Sass,
	Spring,
	Sql,
	Svelte,
	Swift,
	Typescript,
	Ubuntu,
	Vue
} from '@with-me/icons';

<Meta title="assets/Icons" component={ArrowDown} />

export const Template = (args) => <ArrowDown />;

# 아이콘

With Me 아이콘 정리

아이콘을 사용하기 전에 `@with-me/icons 라이브러리`를 먼저 설치해주세요!

> npm install @with-me/icons 또는, <br />
> yarn add @with-me/icons

```js
import { ArrowDown } from '@with-me/icons'
```

위와 같은 방식으로 import 하여 사용하시면 됩니다! <br />
**필요한 아이콘이있으면 요청해주세요!**

<br />
<br />

## 기본 아이콘 목록

- width, height 속성으로 크기를 조정할 수 있습니다.
- fill 속성으로 아이콘의 색상을 변경할 수 있습니다.

<Canvas>
	<Story name="base">
		<ArrowDown />
		<ArrowLeft />
		<ArrowRight />
		<ArrowUp />
    <BackSpace />
		<Close />
		<Code />
		<Github />
		<Heart />
    <Like />
    <Member />
    <Members />
    <Sharing />
		<XCircle />
	</Story>
</Canvas>

<br />

## 기술스택 아이콘 목록

- 마찬가지로 width, height 속성으로 크기를 조정할 수 있습니다.

```ts
// 아래 스토리북에 정리된 아이콘 목록의 코드에서 리액트아이콘만 <ReactIcon /> 으로 가져오고 있습니다.
// 스토리북에서 import name이 겹쳐 오류가 발생했기 때문에 임시로 import { React as ReactIcon } from '@with-me/icons' 로 사용되고 있습니다.
// 실제로 사용하실 땐 아래에 코드처럼 사용하시면 됩니다! (리액트 아이콘만 해당됩니다!)
import { React } from '@with-me/icons'

export default () => {
	return (
		//...
			<React />
		//...
	)
}
```

<Canvas>
	<Story 
    name="skills" 	
    decorators={
      [storyFn => 
        <div style={{
          width: '100%',
          display: 'flex',
          flexWrap: 'wrap',
          gap: '35px'
        }}>
          {storyFn()}
        </div>
      ]
    }>
		<Android />
		<Angular />
		<Aws />
		<C />
		<Cpp />
		<Cs />
		<Css />
		<Deno />
		<Django />
		<Docker />
		<Flutter />
		<Go />
		<Html />
		<Java />
		<Javascript />
		<Kotlin />
		<Kubernetes />
		<Linux />
		<Nodejs />
		<Python />
		<R />
		<ReactIcon />
		<Ruby />
		<Sass />
		<Spring />
		<Sql />
		<Svelte />
		<Swift />
		<Typescript />
		<Ubuntu />
		<Vue />
	</Story>
</Canvas>